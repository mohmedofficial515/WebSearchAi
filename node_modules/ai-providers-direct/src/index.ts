/**
 * Top-level barrel — namespace-per-provider so adding new providers
 * (OpenAI, Anthropic, Gemini, etc.) stays clean:
 *
 *   import { deepseek } from "ai-providers-direct";
 *   const sid = await deepseek.createSession(token);
 *   const r   = await deepseek.chat({ token, sessionId: sid, prompt: "..." });
 */
export * as deepseek from "./providers/deepseek/index.js";
export * as chatgpt  from "./providers/chatgpt/index.js";
export { createLogger } from "./log/logger.js";
export type { Logger, LogLevel, LogData, LogTimer } from "./log/logger.js";
