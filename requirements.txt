# Core — patchright is a drop-in patched Playwright that defeats
# the CDP Runtime.enable leak (the #1 modern bot-detection signal).
# playwright-stealth is removed: it patches at JS level only and
# Cloudflare/Bing/CreepJS catch the double-patching inconsistencies.
patchright>=1.52.0
beautifulsoup4==4.12.3
lxml==5.3.0

# Search backends — API-first so we don't fight Bing/Google CAPTCHAs
tavily-python>=0.5.0   # primary, needs TAVILY_API_KEY (1k free/mo)
ddgs>=9.0.0            # fallback, no key required

# LLM (Mistral)
mistralai==1.2.5
httpx==0.27.2

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5
websockets==14.1

# CLI
typer==0.15.1
rich==13.9.4

# Utilities
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
aiofiles==24.1.0
tenacity==9.0.0
pillow==11.1.0

# Site cloning
html2text==2024.2.26
requests==2.32.3

# Optional OCR for simple captchas
# pytesseract==0.3.13
