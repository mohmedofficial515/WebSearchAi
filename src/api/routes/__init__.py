"""HTTP routers for the chat UI backend.

Each module here exposes a FastAPI `router` that is wired into
`src/api/main.py`. Routes are kept small and feature-focused so the
React frontend can map endpoints to skill cards without grep diving.
"""
